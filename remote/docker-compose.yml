services:
  backend:
    build: .
    restart: unless-stopped
    ports:
      - "8080:8080"
  web:
    build:
      context: .
      dockerfile: Dockerfile.web
    restart: unless-stopped
    ports:
      - "8081:80"
    depends_on:
      - backend
